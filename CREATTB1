CREATE TABLE MARKETDATA
   (
   TICKER  CHAR(6) NOT NULL PRIMARY KEY,
   BID     DECIMAL(7,2),
   ASK     DECIMAL(7,2),
   BIDSIZE SMALLINT,
   ASKSIZE SMALLINT,
   EXCHAGE CHAR(4),
   VOLUME  SMALLINT
   ) IN OZA101DB.OZA101TS;
   CREATE UNIQUE INDEX TICKIDX ON MARKETDATA(TICKER);
